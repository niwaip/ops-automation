# Artifacts 历史归档

本目录保存缺少长期复用价值、但仍可保留为历史样本的验证结果和素材文件。

适用内容：

- 某一次联调导出的 JSON 结果
- 某一次页面检查导出的结构化文本
- 已无法直接说明来源的历史验证产物

处理原则：

- 有通用价值的样例应整理后放回 `../misc/`、`../validation/` 或 `../ui/`。
- 无内容、无用途或重复文件应直接删除，而不是继续归档。
