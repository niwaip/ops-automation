# [OPEN] template-save-values-lost

## 背景

- 现象：页面上编辑 `Template1febbc18Workflow` 的值后，保存成功但再次打开时值丢失。
- 目标：定位值丢失发生在前端提交、后端编译、数据库持久化，还是重新打开时的回显链路。
- 约束：在拿到运行时证据前，不修改业务逻辑；先做最小埋点。

## 假设

1. 前端保存时没有把当前页面值带进请求，导致后端收到的 payload 本身就缺失。
2. 模板型 workflow 改为“后端编译”后，编译接口只保留模板草稿默认值，没有合并页面上的动态 policy/展示值。
3. 后端保存到了数据库，但重新打开时前端初始化流程又用模板草稿覆盖了已保存值。
4. 更新接口写入成功，但读取 `Template1febbc18Workflow` 详情时走了旧字段或错误回填逻辑。
5. `Template1febbc18Workflow` 属于模板编译型 workflow，但当前页面仍按自由编排型 workflow 的状态模型在编辑，导致保存/回显模型不一致。

## 计划

1. 为前端保存动作、后端编译接口、后端持久化与前端回显初始化增加最小调试上报。
2. 让用户复现一次“编辑 -> 保存 -> 重新打开”。
3. 对照 pre-fix 日志，确认值丢失点。
4. 仅在证据明确后做最小修复，并用 post-fix 日志复核。
