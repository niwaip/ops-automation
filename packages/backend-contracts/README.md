# Backend Contracts

Shared backend contracts for the project architecture restructuring.

This directory is a container for focused contract packages. It is not the
runtime implementation layer and should only hold stable cross-service
protocols, DTOs, manifests, and related type definitions.

Current packages include focused contracts for:

- release manifests
- runtime capability structures
- execution events
- agent execution protocol
- agent profiles
