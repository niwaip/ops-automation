/**
 * delegation -> master-planner/delegation
 *
 * The current delegation capability is still implemented by `modules/agent`.
 * This facade establishes the future logical view without moving runtime
 * ownership in this batch.
 */
export { AgentService } from '../../agent';
