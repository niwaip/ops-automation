# @ops/browser-recorder

浏览器能力域中的录制核心包，提供录制底座能力：
- 基础常量与选择器匹配规则（`constants`）
- 持久化定位解析器（`locator/RecorderDurableLocatorResolver`）
- 录制导出执行计划中间表示与构建器（`execution-plan/buildBrowserRecordingExecutionPlan`）
- 循环与人工介入模型契约（`loop`）
