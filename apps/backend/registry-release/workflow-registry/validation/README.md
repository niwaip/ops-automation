# Workflow Validation

Flow、Workflow 与 Activity 的设计时校验实现在 `../src/validation/`，相关 HTTP facade 和特定类型校验协作者也分布在 `../src/flow-template/`、`../src/activity-template/`、`../src/temporal/`。以这些源码目录和包导出为准。

校验注册态输入；最终发布门禁仍由 `release-manager` 负责。
