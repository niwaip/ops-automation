import { useMemo } from 'react';
import {
  buildExecutionListDetailDrawerProps,
  type BuildExecutionListDetailDrawerPropsOptions,
  type ExecutionListDetailDrawerProps,
} from '@/features/executions/lib/executionListDrawerProps';

export function useExecutionListDrawerProps({
  selectedExecutionId,
  isDetailLoading,
  selectedExecution,
  navigate,
  updateExecutionSelection,
  getSkillDisplayName,
  shouldShowSelectedCurrentPhaseInfo,
  selectedExecutionRuntimeSessionId,
  stableSelectedRuntimeSessionNovncUrl,
  isSelectedBrowserExecution,
  isSelectedExecutionActive,
  selectedRuntimeSession,
  selectedExecutionInput,
  selectedExecutionNormalizedResult,
  effectiveSelectedResultJson,
  currentSelectedPhase,
  currentSelectedStep,
  displaySelectedPhases,
  selectedCurrentPhaseIndex,
  selectedCompletedPhaseCount,
  selectedLoopCount,
  shouldShowSelectedExecutionSummary,
  selectedSummaryHeadline,
  selectedLoopSummary,
  waitingInputStep,
  requiredInputs,
  requiredInputGroups,
  submitInputLoading,
  onSubmitWaitingInput,
  onResumeInAi,
  onTakeoverPhase,
  phaseTakeoverLoading,
  shouldShowLegacySteps,
  selectedSteps,
  isStepsLoading,
}: BuildExecutionListDetailDrawerPropsOptions): ExecutionListDetailDrawerProps {
  return useMemo(
    () =>
      buildExecutionListDetailDrawerProps({
        selectedExecutionId,
        isDetailLoading,
        selectedExecution,
        navigate,
        updateExecutionSelection,
        getSkillDisplayName,
        shouldShowSelectedCurrentPhaseInfo,
        selectedExecutionRuntimeSessionId,
        stableSelectedRuntimeSessionNovncUrl,
        isSelectedBrowserExecution,
        isSelectedExecutionActive,
        selectedRuntimeSession,
        selectedExecutionInput,
        selectedExecutionNormalizedResult,
        effectiveSelectedResultJson,
        currentSelectedPhase,
        currentSelectedStep,
        displaySelectedPhases,
        selectedCurrentPhaseIndex,
        selectedCompletedPhaseCount,
        selectedLoopCount,
        shouldShowSelectedExecutionSummary,
        selectedSummaryHeadline,
        selectedLoopSummary,
        waitingInputStep,
        requiredInputs,
        requiredInputGroups,
        submitInputLoading,
        onSubmitWaitingInput,
        onResumeInAi,
        onTakeoverPhase,
        phaseTakeoverLoading,
        shouldShowLegacySteps,
        selectedSteps,
        isStepsLoading,
      }),
    [
      currentSelectedPhase,
      currentSelectedStep,
      displaySelectedPhases,
      effectiveSelectedResultJson,
      getSkillDisplayName,
      isDetailLoading,
      isSelectedBrowserExecution,
      isSelectedExecutionActive,
      isStepsLoading,
      navigate,
      onResumeInAi,
      onSubmitWaitingInput,
      onTakeoverPhase,
      phaseTakeoverLoading,
      requiredInputGroups,
      requiredInputs,
      selectedCompletedPhaseCount,
      selectedCurrentPhaseIndex,
      selectedExecution,
      selectedExecutionId,
      selectedExecutionInput,
      selectedExecutionNormalizedResult,
      selectedExecutionRuntimeSessionId,
      selectedLoopCount,
      selectedLoopSummary,
      selectedRuntimeSession?.state,
      selectedSteps,
      selectedSummaryHeadline,
      shouldShowLegacySteps,
      shouldShowSelectedCurrentPhaseInfo,
      shouldShowSelectedExecutionSummary,
      stableSelectedRuntimeSessionNovncUrl,
      submitInputLoading,
      updateExecutionSelection,
      waitingInputStep,
    ]
  );
}
