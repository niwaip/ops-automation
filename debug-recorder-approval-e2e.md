# [OPEN] recorder-approval-e2e

## 背景

- 目标：对 `http://192.168.100.143/#approvals` 执行录制端到端验证，覆盖循环与条件执行。
- 期望：能录到“循环处理未承认数据，毛利率大于 20% 自动承认，否则人工介入”的一轮稳定样本，并成功导出包含 loop/branch 的 execution plan。
- 实际：真实运行中，录制器在列表页将多个 `詳細` 视为同类文本目标，未稳定命中第一条待处理数据的详情入口，导致循环样本无法正确落库。

## 当前现象

- 页面可正常打开并读取真实审批列表。
- 列表页存在多条重复文案 `詳細`。
- 录制器生成了 `click text=詳細` 类命令，但浏览器执行返回 `Element uid "詳細" not found on page 1.`。
- 当前导出结果只包含 `navigate + 粗略 branch`，尚未形成真实的循环迭代步骤。

## 假设

1. 录制阶段的自然语言解析没有把“第一条待处理案件详情”转成稳定定位器，只退化成了纯文本 `詳細` 点击。
2. 真实 approvals 页面缺少测试中依赖的 `[data-ai-action="detail"]` 一类语义化属性，导致循环导出预期与真实 DOM 不一致。
3. chrome-devtools 快照里的 `uid` 可以执行，但当前 recorder chat 未把快照中的 `uid/ref` 回填进生成命令。
4. 列表页点击前后的 snapshot/observation 合并逻辑丢失了可用于 disambiguation 的候选信息，导致没有走歧义确认而是直接执行错误命令。
5. 条件 token `[条件分歧]` 目前仅影响导出意图，不足以在录制阶段约束点击目标，因此端到端问题首先发生在“首个详情定位”而不是 branch 生成阶段。

## 计划

1. 收集当前会话、执行结果和浏览器快照证据。
2. 给 recorder 录制链添加最小调试埋点，只记录候选生成、歧义判断和最终命令定位。
3. 复现 `approvals` 录制流程，对比埋点前后证据。
4. 基于证据做最小修复，再重新验证导出 loop/branch。

## 证据更新

- `pre-fix` 日志证明：
  - observation 的 `buttons/candidates` 为空。
  - `点击第一条保留中案件的详情` 被解析成 `click { text: "詳細" }`。
  - flow 未进入歧义确认，直接执行坏命令。
- `pre-fix` 运行结果证明：
  - 浏览器执行返回 `Error: Element uid "詳細" not found on page 1.`。
- 已实施最小修复：
  - `RecorderSnapshotService` 兼容 `chrome-devtools` 的 `uid=...` 快照格式。
  - 新增单测验证 `uid` 按钮可被观察，并且重复 `詳細` 文本会被重写成首个 uid 目标。
- `post-fix` 运行结果证明：
  - approvals 列表页已经能提取出 `詳細` 按钮候选，如 `ref=9_39 / 9_46 / 9_53 ...`。
  - “点击第一条保留中案件的详情” 已改写成 `target=9_39` 并成功跳转到详情页。

## 当前剩余阻塞

1. `条件分歧` token 已接入后端识别：
   - `chat` 遇到 `[条件分歧] ...` 时只记录条件说明，不再误判成页面配置/审批动作。
   - 实际返回为“已记录条件分歧…不会在录制阶段直接执行浏览器动作，会在导出 templateSteps 时生成条件分支。”
2. 导出阶段新增两处最小修复：
   - 当本轮录制没有真的点击“承认”时，不再误把唯一的“返回一览”步骤切掉。
   - 分支分析优先使用“条件描述附近”的 detail observation，不再错误使用回到列表后的 observation。
3. 真实 `approvals` 端到端复验结果（`recorder-debug-e2e-approval-conditional-7`）：
   - `打开 approvals -> [循环对象:当前列表] -> [循环开始] 点击第一条保留中详情 -> [条件分歧] 毛利率大于20%自动承认，否则人工介入 -> 返回一览页面 [循环结束] -> export`
   - 导出结果已包含：
     - `navigate`
     - `click`（进入详情）
     - `read_value`
     - `branch`
     - `click`（`承認する (Approve)`）
     - `click`（返回一览）
   - 说明循环 + 条件 + 导出主链路已经真实跑通。
4. 当前剩余风险：
   - 真实会话中“进入详情 / 返回一览”仍可能保留运行时 `uid/ref` 风格定位（如 `32_39`、`37_0`），功能上可导出，但后续如要进一步提升模板跨会话稳定性，仍应继续把这类定位收敛为更稳定的 row-scoped selector / ref 语义。
