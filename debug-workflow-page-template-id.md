# Debug Session: workflow-page-template-id

- Status: OPEN
- Created: 2026-05-28
- Symptom: 工作流生成页面中，模板 ID `1febbc18-1f17-4c49-a4b2-9bfb38fffeaf` 没有按预期出现，需要确认是在页面提交、后端保存、接口返回还是页面回显阶段丢失。

## Hypotheses

1. 工作流生成页面提交时没有把模板 ID 放进请求体，或字段名与后端契约不一致。
2. 后端接收到模板 ID，但在 DTO / service 映射时被忽略或重命名了。
3. 后端保存了模板 ID，但查询详情/列表接口返回 DTO 时没有带回该字段。
4. 前端拿到了模板 ID，但页面状态映射或展示组件使用了错误字段。
5. 页面里“两个默认值”逻辑覆盖了用户选中的模板 ID，导致回显时显示为空或旧值。

## Plan

1. 梳理工作流生成页面相关前端请求、后端 controller/service、返回 DTO。
2. 只加最小调试埋点，记录“提交入参 / 后端接收 / 保存后 / 查询返回”。
3. 用模板 ID `1febbc18-1f17-4c49-a4b2-9bfb38fffeaf` 复现并对照日志确认丢失点。
4. 再基于证据做最小修复，并验证默认值逻辑。
